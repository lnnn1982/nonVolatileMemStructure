# NonVolatileMemStructure

Implementations of data structures for non-volatile memory. 

## testPMDK
Data structures implemented with PMDK library(http://pmem.io/pmdk/) to manage objects allocations of non-volatile memory.
